
server:
  port: 11081
  servlet:
    context-path: /dboj
spring:
  datasource:
    url: jdbc:postgresql://glab2.hpc.mylab.cc:11432/cs309proj
    username: dboj
    password: dboj2020
    driver-class-name: org.postgresql.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 15
      auto-commit: true
      idle-timeout: 30000
      pool-name: DatebookHikariCP
      max-lifetime: 120000
      connection-timeout: 30000
      connection-test-query: SELECT 1
  jpa:
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
    hibernate:
      ddl-auto: create #if use create, we will build new table and clean all the date.
    open-in-view: false
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect